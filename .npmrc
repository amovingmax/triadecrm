# Configuração do pnpm para o monorepo (sem segredos — tokens de registro ficam fora do repo).
auto-install-peers=true
strict-peer-dependencies=false
# Recusa instalar com Node/pnpm fora do "engines" do package.json (Node 22, pnpm 11).
engine-strict=true
